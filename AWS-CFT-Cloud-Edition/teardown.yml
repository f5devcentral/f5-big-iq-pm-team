---
# Delete VPC and keypair indicated in the aws-resources.yml file
- name: Delete AWS Resources
  hosts: localhost
  vars_files: 
    - ../config.yml # this file is generated by ~/setup/aws-prerequisite.yml
  tasks:
    - name: Delete CF Stack
      cloudformation:
        stack_name: "{{PREFIX}}-cf-stack"
        state: absent
...